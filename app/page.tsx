"use client";
import Link from "next/link";
import { Bus, CheckCircle2, Clock3, GraduationCap, MapPin, Route as RouteIcon, School, ShieldCheck, Users } from "lucide-react";
import { DashboardLayout } from "@/components/layout/DashboardLayout";
import { RecentActivity } from "@/components/ui/RecentActivity";
import { StatCard } from "@/components/ui/StatCard";
import { useNexoData } from "@/components/providers/NexoDataProvider";

export default function Home() {
  const { schools, students, buses, drivers, routes } = useNexoData();
  const activeRoutes = routes.filter(route => route.status === "active");
  const activeBuses = buses.filter(bus => bus.status === "active");
  return <DashboardLayout title="Operations Center"><section className="overflow-hidden rounded-3xl bg-slate-950 px-6 py-7 text-white shadow-xl sm:px-8"><div className="flex flex-wrap items-start justify-between gap-5"><div><p className="text-sm font-semibold tracking-wide text-teal-300">NEXO OPERATIONS</p><h2 className="mt-2 text-2xl font-bold tracking-tight sm:text-3xl">Every ride, clearly connected.</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-300">A single view for school routes, vehicles, drivers, and students—built to keep every journey safe, reliable, and on schedule.</p></div><Link href="/routes" className="rounded-xl bg-teal-400 px-4 py-2.5 text-sm font-bold text-slate-950 hover:bg-teal-300">Review live routes</Link></div><div className="mt-7 grid gap-4 sm:grid-cols-3"><Signal label="Routes on time" value={`${activeRoutes.length}/${routes.length}`} icon={Clock3}/><Signal label="Vehicles in service" value={`${activeBuses.length}/${buses.length}`} icon={Bus}/><Signal label="Students assigned" value={String(students.filter(student => student.transportationStatus === "assigned").length)} icon={CheckCircle2}/></div></section><section className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Link href="/schools"><StatCard label="Schools" value={String(schools.length)} icon={School} tone="bg-violet-100 text-violet-700"/></Link><Link href="/students"><StatCard label="Students" value={String(students.length)} icon={GraduationCap} tone="bg-teal-100 text-teal-700"/></Link><Link href="/buses"><StatCard label="Fleet vehicles" value={String(buses.length)} icon={Bus} tone="bg-amber-100 text-amber-700"/></Link><Link href="/drivers"><StatCard label="Drivers" value={String(drivers.length)} icon={Users} tone="bg-sky-100 text-sky-700"/></Link></section><section className="mt-8 grid gap-6 xl:grid-cols-[1.45fr_1fr]"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="flex items-center justify-between"><div><h2 className="text-lg font-semibold">Route readiness</h2><p className="mt-1 text-sm text-slate-500">Today’s active routes and their assigned teams.</p></div><Link href="/routes" className="text-sm font-semibold text-teal-700">View all routes</Link></div><div className="mt-5 space-y-3">{activeRoutes.slice(0,4).map(route => <article key={route.id} className="flex flex-wrap items-center justify-between gap-3 rounded-xl border border-slate-100 bg-slate-50 p-4"><div className="min-w-48"><p className="font-semibold text-slate-900">{route.name}</p><p className="mt-1 flex items-center gap-1 text-sm text-slate-500"><MapPin size={14} className="text-teal-600"/>{route.pickupLocation || "Pickup not set"} → {route.dropoffLocation || "Drop-off not set"}</p></div><div className="text-sm text-slate-600"><p>{buses.find(bus => bus.id === route.busId)?.name || "Bus pending"}</p><p className="mt-1">{drivers.find(driver => driver.id === route.driverId)?.name || "Driver pending"}</p></div><span className="inline-flex items-center gap-1.5 rounded-full bg-emerald-100 px-2.5 py-1 text-xs font-bold text-emerald-800"><CheckCircle2 size={14}/>Ready</span></article>)}</div></section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><h2 className="text-lg font-semibold">Service confidence</h2><div className="mt-5 space-y-5"><Metric label="Active route coverage" value={`${activeRoutes.length} active routes`} percent={routes.length ? Math.round(activeRoutes.length / routes.length * 100) : 0}/><Metric label="Fleet readiness" value={`${activeBuses.length} vehicles active`} percent={buses.length ? Math.round(activeBuses.length / buses.length * 100) : 0}/><Metric label="Student assignment" value={`${students.filter(student => student.transportationStatus === "assigned").length} students assigned`} percent={students.length ? Math.round(students.filter(student => student.transportationStatus === "assigned").length / students.length * 100) : 0}/></div><div className="mt-6 flex gap-3 rounded-xl bg-teal-50 p-4 text-sm text-teal-900"><ShieldCheck className="shrink-0 text-teal-700" size={20}/><p>Operations data is connected across schools, routes, vehicles, drivers, and students.</p></div></section></section><div className="mt-8 max-w-3xl"><RecentActivity/></div></DashboardLayout>;
}
function Signal({ label, value, icon: Icon }: { label:string; value:string; icon:typeof Clock3 }) { return <div className="rounded-2xl border border-white/10 bg-white/5 p-4"><Icon size={18} className="text-teal-300"/><p className="mt-3 text-2xl font-bold">{value}</p><p className="mt-1 text-sm text-slate-300">{label}</p></div>; }
function Metric({ label, value, percent }: { label:string; value:string; percent:number }) { return <div><div className="flex justify-between gap-3 text-sm"><span className="font-medium text-slate-700">{label}</span><span className="text-slate-500">{value}</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-teal-500" style={{ width: `${percent}%` }}/></div></div>; }
